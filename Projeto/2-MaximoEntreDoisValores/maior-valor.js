// Função para usar 2 numeros e retorna o maior valor entre eles

// Definindo valores
let valorMaior = max(5,10);
console.log(valorMaior);
// Função para comparar o número maior
function max(numero1,numero2){
    if(numero1 > numero2)
    return numero1;
    else return numero2;
}
// Com operador ternário
let valorMenor = min(15,34)
console.log(valorMenor)
function min(numero3,numero4) {
    return numero3 < numero4 ? numero3: numero4;
}